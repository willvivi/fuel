# name: Docker Image CI Front

# on:
#   push:
#     branches: [ "develop" ]
#   pull_request:
#     branches: [ "develop" ]

# jobs:

#   build-front:

#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v3
#     - name: Build the Docker image
#       working-directory: ./front
#       run: docker build . --file Prod.Dockerfile --tag fuel-front:$(date +%s)

#   build-api:

#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v3
#     - name: Build the Docker image
#       working-directory: ./api
#       run: docker build . --file Prod.Dockerfile --tag fuel-api:$(date +%s)

#   build-cron:

#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v3
#     - name: Build the Docker image
#       working-directory: ./cron
#       run: docker build . --file Prod.Dockerfile --tag fuel-cron:$(date +%s)


# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# GitHub recommends pinning actions to a commit SHA.
# To get a newer version, you will need to update the SHA.
# You can also reference a tag or branch, but the action may change without warning.

name: Publish Docker images

on:
   pull_request:
      branches: [ "develop" ]
   push:
      branches: [ "develop" ]

jobs:
  push_to_registry_front:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3
      
      - name: Log in to Docker Hub
        uses: docker/login-action@f054a8b539a109f9f41c372932f1ae047eff08c9
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: <FRONT> Extract metadata (tags, labels) for Docker
        id: meta_front
        uses: docker/metadata-action@98669ae865ea3cffbcbaa878cf57c20bbf1c6c38
        with:
          images: willvivi/fuel-front
      
      - name: <API> Extract metadata (tags, labels) for Docker
        id: meta_api
        uses: docker/metadata-action@98669ae865ea3cffbcbaa878cf57c20bbf1c6c38
        with:
          images: willvivi/fuel-api
      
      - name: <CRON> Extract metadata (tags, labels) for Docker
        id: meta_cron
        uses: docker/metadata-action@98669ae865ea3cffbcbaa878cf57c20bbf1c6c38
        with:
          images: willvivi/fuel-cron
      
      - name: <FRONT> Build and push Docker image
        uses: docker/build-push-action@ad44023a93711e3deb337508980b4b5e9bcdc5dc
        with:
          context: ./front
          file: Prod.Dockerfile
          push: true
          tags: ${{ steps.meta_front.outputs.tags }}
          labels: ${{ steps.meta_front.outputs.labels }}

      - name: <API> Build and push Docker image
        uses: docker/build-push-action@ad44023a93711e3deb337508980b4b5e9bcdc5dc
        with:
          context: ./api
          file: Prod.Dockerfile
          push: true
          tags: ${{ steps.meta_api.outputs.tags }}
          labels: ${{ steps.meta_api.outputs.labels }}

      - name: <CRON> Build and push Docker image
        uses: docker/build-push-action@ad44023a93711e3deb337508980b4b5e9bcdc5dc
        with:
          context: ./cron
          file: Prod.Dockerfile
          push: true
          tags: ${{ steps.meta_cron.outputs.tags }}
          labels: ${{ steps.meta_cron.outputs.labels }}